version: "3.1"

services:
  audio_hunter_bot:
    container_name: audio_hunter_bot
    environment:
      BOT_TOKEN: "your_bot_token"
      BOT_ADMIN_USER_ID: "your_user_id"
      SEARCH_RESULT_COUNT: "5"
      DATABASE_PATH: "data/sounds_database.db"
      TOP_USER_REQUESTS_COUNT: "5"
      FEEDBACK_MESSAGE: |
        Дорогие друзья! 🐾

        Рад сообщить, что бот стал ещё интереснее и полезнее! Теперь вместе со звуками вы будете получать красивые картинки, иллюстрирующие каждый звук.
        Например, вместе с «му» коровы вы увидите яркую и веселую картинку с коровой. 🎨🐄

        Спасибо вам за то, что пользуетесь моим ботом! ❤️

        Если у вас есть предложения, пожелания или идеи по улучшению работы бота, обязательно пишите мне: @Slabodenyuk_Anatoly
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"